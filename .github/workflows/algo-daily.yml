name: "Daily 자료구조 및 알고리즘"

on:
  schedule:
    - cron: '20 20 * * *'  # 매일 오전 5:20 KST
  workflow_dispatch:

jobs:
  generate-note:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull latest changes
        run: git pull --rebase origin master

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install openai==1.82.0

      - name: Generate today's algo note
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python generate.py topics_algo.json

      - name: Commit and push note
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add output/algo/ topics_algo.json
          git diff --cached --quiet || git commit -m "📚 자료구조/알고리즘: $(TZ='Asia/Seoul' date '+%Y-%m-%d')"
          git pull --rebase origin master
          git push
